#!/bin/sh
# (C) Copyleft by Hrtk

_dmenu(){
	cat - | dmenu -l 10 -fn "$FONT" -i
}

FONT="Source\ Code\ Pro-20"
CONFIG=~/.clipbox
if [[ ! -f $CONFIG ]]; then
	>&2 echo "No $CONFIG file found"
	echo "No $CONFIG file found" | _dmenu 
	exit 10
fi

 _dmenu < $CONFIG | xargs -d "\n" xdotool type
